import AIMetricCard from "../common/AIMetricCard";

/** @deprecated Use AIMetricCard (default light theme) */
export default function AIMetricCardDark(props) {
  return <AIMetricCard {...props} />;
}
